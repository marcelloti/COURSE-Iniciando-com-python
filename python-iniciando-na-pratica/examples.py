from helper import printComment

printComment("School of net - Examples")

printComment("Wait to user input something")
response = input("Type something: ")
print(response)

printComment("Print the type of the variable")
print(type(response))
